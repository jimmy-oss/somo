export interface new_user {
    username: string;
    email: string;
    password1: string;
    password2: string;
}

export interface user_login_data {
    username: string;
    email: string;
    password: string;
}